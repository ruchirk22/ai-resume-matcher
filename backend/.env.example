# Example environment variables for ai-resume-matcher backend
# Copy this file to `.env` locally and fill in the real values (do NOT commit real secrets).

# PostgreSQL connection string (Render / Supabase / other managed Postgres)
# Example: postgresql://user:password@host:5432/dbname
DATABASE_URL=

# API key for Gemini / Google Generative AI (or other LLM provider)
GEMINI_API_KEY=

# Long random string used to sign JWT tokens (keep secret)
JWT_SECRET=

# (Optional) Frontend URL used for CORS in production, e.g. https://ai-resume-matcher-frontend.onrender.com
FRONTEND_URL=

# Directory to store uploaded files locally (note: Render filesystem is ephemeral)
UPLOAD_DIR=./uploads

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
